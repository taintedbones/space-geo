import { Typography } from '@material-ui/core';

function GeoMoonPage() {
    return(
        <div>
            <Typography>Geology of the Moon Page</Typography>
        </div>
    );
}

export default GeoMoonPage;