import { Typography } from '@material-ui/core';

function GeoMoonPage() {
    return(
        <div>
            <Typography variant="h4">Geology of the Moon Page</Typography>
            <Typography variant="body1">
                Add text here.
            </Typography>
        </div>
    );
}

export default GeoMoonPage;